# S01: S01

**Goal:** Ensure the persisted activateHotkey in settings.json is honored on cold start instead of being overridden by the default. Single-line Rust fix plus an observable error path.
**Demo:** User sets a custom hotkey, saves, quits via tray, relaunches. The dev console prints `Registered activate hotkey: <custom>` (not the default), and the custom hotkey triggers the overlay on first press.

## Must-Haves

- Complete the planned slice outcomes.

## Verification

- Run the task and slice verification checks for this slice.

<tasks>
- [x] **T01**: Switch get_store to store at setup-time hotkey read _(XS)_
  In src-tauri/src/main.rs, replace app.get_store("settings.json") with app.store("settings.json") inside the setup() closure that reads activateHotkey for initial registration. Update the match arm from Some/None to Ok/Err. Log the Err path via eprintln so a disk-load failure is visible rather than silently falling back to the default hotkey.
  - Files: `src-tauri/src/main.rs`
  - Verify: User runs npm run tauri:dev, sets a non-default hotkey (e.g. F6) in Settings, saves, quits via tray → Quit, relaunches. Console log line must read 'Registered activate hotkey: F6' (or whatever was saved), not the default. Pressing the saved hotkey must trigger the overlay.
</tasks>

## Files Likely Touched

- src-tauri/src/main.rs
